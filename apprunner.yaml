version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm run install-all
      - npm run build
run:
  command: npm start
  network:
    port: 3001
    env:
      PORT: "3001"
      NODE_ENV: "production"
